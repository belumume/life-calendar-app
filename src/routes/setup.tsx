import { Title } from "@solidjs/meta";
import Setup from "../components/Setup";

export default function SetupPage() {
  return (
    <main class="container">
      <Title>Setup - MyLife Calendar</Title>
      <Setup />
    </main>
  );
}